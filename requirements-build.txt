# Direct release-tool dependencies. Transitive packages are intentionally
# resolved by pip so they remain compatible with these two pinned tools.
splunk_add_on_ucc_framework==6.5.3
splunk-appinspect==4.3.0
